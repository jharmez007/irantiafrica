import { AdminReturns } from "@/components/returns/admin-returns";
export const metadata = {
  title: "Returns & refunds | IRANTI Africa",
  robots: { index: false, follow: false },
};
export default function Page() {
  return <AdminReturns />;
}
